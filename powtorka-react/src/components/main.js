const input = document.getElementById('inputId').value // zawsze pobierze wartości poczatkową inputa
// z momentu jego renderowanie. zawsze będzie równa pustemu stringowi



function getInputVal () {
    const inputValue = document.getElementById('inputId').value 
    // pobierze wartość z inputa momentu kiknięcia
}